/*
This JAVASCRIPT file will focus on node lists. 

A second aspect to document object model, which can return more than one element.
*/


var listElements = document.getElementsByTagName('li'); 
document.write(listElements.length); 

// A nodelist query is created by getting  all the li elements

// The length of the elements are then written out to the DOM. 