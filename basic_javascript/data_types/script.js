var number1, number2, number3;

number1 = 01;
number2 = 02; 
number3 = 03;

// the lines above create three variables with their names in one line .
// values are then assigned to them of the number type.

var isOn = true;

var isOff = false;

// two variables are declared under one line for each of them.  the value of them are booleans.

var name1;
name1 = 'Jonathan Mohabir ';

var name2;
name2 = 'Future GOOD ';

// two variables are created and they are of the string type.

var color;

color = ['white', 'black', 'orange'];

// an array is created of all string types assigned to the color variable name.
